# 長編小説制作プロトタイプ：作業規約

## 目的

このリポジトリは、エージェントが長編小説を一話ずつ継続して制作するための外部記憶である。ユーザーから複数話をまとめて依頼された場合も、一話の執筆と管理情報の更新を一単位として、指定された回数だけ順番に繰り返す。
最初の目標は、伏線管理や高度な構成管理ではなく、人物・場所・時間・状況の連続性を保ちながら複数話を書き続けられることとする。

実際の本文執筆は、ユーザーが明示的に依頼した場合だけ開始する。

## 情報の優先順位

情報が食い違う場合は、次の順で優先する。

1. ユーザーの最新の明示的な指示
2. `start.md`
3. `canon/` 配下の確定設定
4. `state/current.md`
5. `summaries/running-summary.md`
6. `summaries/episodes/` 配下の各話要約
7. `manuscript/` 配下の本文

ただし、本文と管理ファイルが食い違う場合は黙って修正しない。差異をユーザーへ報告し、どちらを正史にするか確認する。

## ファイルの役割

- `start.md`: 作品の出発条件と執筆パラメータ
- `canon/world.md`: 世界の不変または長期的な設定
- `canon/characters.md`: 人物の基本属性、性格、話し方
- `canon/locations.md`: 場所そのものの構造、距離、特徴
- `state/current.md`: 現在時点の時間、人物、関係、所持品、位置
- `summaries/running-summary.md`: 毎回読み込む短い累積あらすじ
- `summaries/episodes/NNN.md`: 各話終了時点の保存用要約
- `manuscript/NNN.md`: 各話本文
- `templates/`: 新しい作品・話を作る際の書式

## 固定情報と可変情報を混ぜない

- 生年月日、基本性格、平常時の口調などは `canon/characters.md` に置く。
- 現在地、負傷、感情、所持品、一時的な話し方の変化などは `state/current.md` に置く。
- 場所の構造や恒常的な特徴は `canon/locations.md` に置く。
- 人物や重要物品の現在位置は `state/current.md` に置く。

一話だけで起きた一時的変化を、人物の恒常的な性格や口調として自動的に確定してはならない。

## 本文執筆前の手順

1. `start.md` を読む。
2. `canon/` 配下を読む。
3. `state/current.md` を読む。
4. `summaries/running-summary.md` を読む。
5. 直近三話までの各話要約を読む。
6. 必要な場合だけ直近の本文を読む。
7. 次話の番号、目的、開始地点、開始時刻、登場人物を確定する。
8. 不明点が物語を大きく分岐させる場合は、単話の執筆開始前または複数話分の連続執筆を開始する前にユーザーへ確認する。

資料を読まず、会話履歴だけを頼りに本文を書いてはならない。

## 一話の執筆ルール

- 文字数は `start.md` の指定に従う。
- 指定が空の場合は約3,000字を基準とし、2,700～3,300字を許容範囲とする。
- 一話には、最低でも「開始時の状況」「人物の行動または選択」「終了時の変化」を含める。
- 設定資料にない細部は、既存設定と矛盾しない範囲で創作してよい。
- 新しく創作した事実は、本文に実際に書かれたものだけを確定情報として扱う。
- 読者向け本文に、管理用語、ファイル名、エージェントの判断過程を混入させない。
- ユーザーが依頼していない大規模な時間跳躍、舞台変更、主人公変更を行わない。

## ユーザーの依頼単位とエージェントの処理単位

- ユーザーは、一話だけを依頼して内容を確認してから続きを依頼してもよい。
- ユーザーは、「全20話」など複数話を一度に依頼してもよい。
- エージェントの処理単位は、ユーザーの依頼話数にかかわらず常に一話とする。
- 複数話を依頼された場合は、指定範囲全体の流れと各話の役割を最初に整理する。
- その後、「一話を執筆する、要約と管理ファイルを更新する、本文と管理情報を照合する」という一話分の工程を、指定話数に達するまで繰り返す。
- 複数話の本文を先にまとめて執筆し、後から要約や管理ファイルを一括更新してはならない。
- 次話は、必ず直前の話で更新した最新状態を読んでから執筆する。
- ユーザーが途中確認を指定していない限り、各話の完成ごとに作業を止めて確認を求めない。
- 連続執筆の開始前に、作品全体を大きく分岐させる未決定事項が残っている場合だけユーザーへ確認する。

## 一話の完成後に必ず行うこと

本文を保存した後、次の順で管理情報を更新する。

1. `summaries/episodes/NNN.md` を新規作成する。
2. `summaries/running-summary.md` を更新する。
3. `state/current.md` を、本文終了時点の状態へ更新する。
4. 新人物が登場した場合は `canon/characters.md` に追記する。
5. 新しい恒常的な場所が登場した場合は `canon/locations.md` に追記する。
6. 世界の恒常的な事実が本文で確定した場合だけ `canon/world.md` に追記する。
7. 本文と更新した管理ファイルを照合する。

本文だけを書いて管理ファイルを更新せずに作業を終了してはならない。

## 要約のルール

各話要約には次の情報を含める。

- その話で何が起きたかを示す短い要約
- 開始地点と終了地点
- 経過時間または終了時刻
- 登場人物
- 人物ごとの重要な変化
- 新しく登場した人物、場所、重要物品
- 次話がそのまま引き継ぐ未解決の目前の状況

ここで扱う「未解決の状況」は、進行中の会話、目前の危険、約束、当面の目的などである。伏線の意図、回収予定、謎の設計は現段階では管理しない。

要約に本文未記載の解釈や将来の予定を混ぜない。

## 累積あらすじのルール

`summaries/running-summary.md` は、すべての各話要約を連結したものではない。現在の話を続けるために必要な経緯を圧縮した文書とする。

- 古い出来事は原因と結果が分かる範囲で圧縮する。
- 現在の人間関係や目的につながる出来事は残す。
- 現在無関係に見える細部は、各話要約に残して累積あらすじからは外してよい。
- 原則として日本語4,000字以内を目安にする。
- 圧縮によって確定事実を反転、合成、推測してはならない。

## 人物管理のルール

人物ごとに、少なくとも次を管理する。

- 表記名と読み
- 物語上の役割
- 外見上の識別点
- 基本性格と価値観
- 一人称、呼称、台詞の特徴
- 避けるべき話し方
- 他人物との基本関係
- 現在地
- 現在の身体状態と感情状態
- 現時点の当面の目的
- 重要な所持品

基本情報は `canon/characters.md`、現在情報は `state/current.md` に記録する。

## 場所管理のルール

場所ごとに、少なくとも次を管理する。

- 正式名称と別名
- 上位地域
- 場所の種類と規模
- 恒常的な外観、設備、雰囲気
- 出入口と主要な内部区画
- 他の主要地点との位置関係や通常の移動時間
- 通常そこにいる人物または集団

移動を書く際は、直前の人物位置、利用可能な移動手段、経過時間が矛盾しないか確認する。

## 状態管理のルール

`state/current.md` は物語の「最新セーブデータ」として扱う。過去の状態を併記して肥大化させず、各話終了時点の現在だけを書く。過去は本文と各話要約に残す。

少なくとも次を保持する。

- 最新話番号
- 作中日時
- 直前の終了場所と状況
- 各人物の現在地、身体、感情、目的
- 人物間の現在の関係
- 重要物品の保持者または所在
- 次話開始時にも継続している目前の状況

## 禁止事項

- 過去の本文や各話要約を上書きしない。
- 本文にない出来事を「起きたこと」として台帳へ追加しない。
- 不明な設定を既知の正史であるかのように補完しない。
- 要約を根拠に、本文と矛盾する細部を作らない。
- 人物の口調を一話ごとに安易に変更しない。
- プロトタイプ段階で伏線台帳や回収計画を勝手に追加しない。

## プロトタイプの成功条件

まず十話程度を対象とし、次を満たせるか検証する。

- 前話の終了状態から自然に次話が始まる。
- 人物の基本性格、呼称、話し方が維持される。
- 人物と重要物品の位置が矛盾しない。
- 移動と時間経過が不自然でない。
- 新人物、新場所、状態変化が管理ファイルへ反映される。
- 累積あらすじだけが際限なく肥大化しない。
- 管理作業が本文制作を圧迫しすぎない。

問題が実際に発生した場合、その種類に応じて管理ファイルを追加する。予想だけを理由に管理項目を増やさない。
